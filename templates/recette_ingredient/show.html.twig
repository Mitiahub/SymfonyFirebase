{% extends 'base.html.twig' %}

{% block title %}RecetteIngredient{% endblock %}

{% block body %}
    <h1>RecetteIngredient</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ recette_ingredient.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_recette_ingredient_index') }}">back to list</a>

    <a href="{{ path('app_recette_ingredient_edit', {'id': recette_ingredient.id}) }}">edit</a>

    {{ include('recette_ingredient/_delete_form.html.twig') }}
{% endblock %}
